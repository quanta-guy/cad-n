# CAD-N development / test / build dependencies.
-r requirements.txt
pytest==9.0.3
matplotlib==3.10.9   # benchmarks + visual-check rendering only (NOT a runtime dep)
pillow==12.2.0       # icon generation + image diffing for visual tests
pyinstaller>=6.11    # .exe packaging
